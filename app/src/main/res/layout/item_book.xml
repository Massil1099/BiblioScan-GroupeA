<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginVertical="8dp"
    android:layout_marginHorizontal="12dp"
    app:cardCornerRadius="12dp"
    app:cardElevation="6dp"
    app:cardUseCompatPadding="true"
    android:clickable="true"
    android:focusable="true"
    android:foreground="?attr/selectableItemBackground"
    app:strokeColor="?attr/colorOnBackground"
    app:strokeWidth="0dp"
    app:cardBackgroundColor="?attr/colorSurface"
    android:importantForAccessibility="yes"
    android:contentDescription="Carte du livre avec image, titre et auteur">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:padding="12dp"
        android:gravity="center_vertical">

        <!-- Image du livre -->
        <ImageView
            android:id="@+id/book_image"
            android:layout_width="70dp"
            android:layout_height="100dp"
            android:scaleType="centerCrop"
            android:src="@drawable/result_placeholder"
            android:background="@drawable/rounded_edittext_background"
            android:elevation="2dp"
            android:contentDescription="Image de couverture du livre" />

        <!-- Infos texte -->
        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="1"
            android:orientation="vertical"
            android:layout_marginStart="16dp"
            android:gravity="center_vertical"
            android:importantForAccessibility="yes">

            <TextView
                android:id="@+id/book_title"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="Titre du Livre"
                android:textColor="?attr/colorOnBackground"
                android:textSize="17sp"
                android:textStyle="bold"
                android:maxLines="2"
                android:ellipsize="end"
                android:contentDescription="Titre du livre : Titre du Livre" />

            <TextView
                android:id="@+id/book_author"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="Auteur inconnu"
                android:textColor="?attr/colorOnBackground"
                android:textSize="14sp"
                android:layout_marginTop="6dp"
                android:maxLines="1"
                android:ellipsize="end"
                android:contentDescription="Auteur : Auteur inconnu" />
        </LinearLayout>
    </LinearLayout>
</com.google.android.material.card.MaterialCardView>
